
 pipeline {
     agent any
     stages {
        stage('Checkout') {
           steps {
             // Clonar el repositorio desde Git
             checkout scm
           }
         }
         stage('Run app') {
             steps {
                 sh 'cd crm_backend/ &&'
                 sh 'sbt run'
             }
         }         
     }
 }
