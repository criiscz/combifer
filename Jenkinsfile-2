
 pipeline {
     agent any
     stages {        
         stage('Run app') {
             steps {
                 sh 'cd crm_backend/ &&'
                 sh 'sbt run'
             }
         }         
     }
 }
